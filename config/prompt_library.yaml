base_templates:
  latin_afro_house: |
    {tempo}bpm, {key}, afro house,
    percussion: {percussion_style},
    mood: {mood},
    avoid: {avoid_patterns}

hostile_derived: []  # Populated by hostile_reference.py

avoid_patterns:
  - "four-on-the-floor without syncopation"
  - "generic tropical house stabs"
  - "predictable build-drop structure"
